mutation signUp($input: UsersPermissionsLoginInput!) {
  login(input: $input) {
    user {
      id
      username
      email
      role {
        id
        name
        description
      }
    }
    jwt
  }
}
